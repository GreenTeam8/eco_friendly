class User {
   String? id;
   String? email;
   String? password;
   String? phoneNumber;
   String? address;
   bool? isAdmin = false;

  User({this.id, this.email, this.password, this.phoneNumber, this.address, this.isAdmin});

}